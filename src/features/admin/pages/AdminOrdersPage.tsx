// PUBLIC_INTERFACE
export function AdminOrdersPage() {
  /** Placeholder for read-only orders admin (Phase 5). */
  return (
    <div>
      <h1>Admin · Orders</h1>
      <p className="muted">
        Placeholder page. Phase 5 will add order listing and detail views (as supported by backend).
      </p>
    </div>
  );
}
