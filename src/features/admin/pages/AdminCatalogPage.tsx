// PUBLIC_INTERFACE
export function AdminCatalogPage() {
  /** Placeholder for read-only catalog admin (Phase 5). */
  return (
    <div>
      <h1>Admin · Catalog</h1>
      <p className="muted">
        Placeholder page. Phase 5 will add read-only catalog views driven by catalog APIs.
      </p>
    </div>
  );
}
